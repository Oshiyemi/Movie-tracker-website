[build]
  command = "pnpm run build"

[functions]
  node_bundler = "esbuild"

# Optional static-export fallback (disabled by default).
# Use this context only for fully static deploys (no SSR/API routes):
# [context.static-export]
#   command = "NETLIFY_NEXT_EXPORT=true pnpm run build"
#   publish = "out"